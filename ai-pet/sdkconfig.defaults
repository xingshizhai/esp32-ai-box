# AI pet product defaults. Board defaults are loaded separately.
CONFIG_ENABLE_LOCAL_OFFLINE_WAKEUP=y
CONFIG_MODEL_IN_FLASH=y
CONFIG_SR_WN_WN9_NIHAOXIAOZHI_TTS=y
# Keep 小智 awake while anti-chat completes ASR, LLM, and TTS for its next turn.
CONFIG_CONVERSATION_FOLLOWUP_MS=30000
